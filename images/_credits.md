# Image credits

このディレクトリの画像は `scripts/fetch-images.sh` を実行して
Wikimedia Commons / Wikipedia から取得します。

実行後、本ファイルが取得した各画像のソース URL で自動的に上書きされます。

画像が無いアトラクションは、アプリ／旅程資料側でフォールバック UI
（絵文字 + グラデーション）として表示されます。
